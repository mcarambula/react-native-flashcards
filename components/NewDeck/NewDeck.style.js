import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    questionText: {
        fontSize: 38,
        textAlign: 'center',
    },
    error: {
        color: 'red'
    }
});

export default styles;
